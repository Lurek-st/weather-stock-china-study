# 研究协议 · Protocol v1.0.2

状态：**冻结候选，完成5个运行日试验后正式冻结**
架构：ChatGPT计划任务采集；Codex每周校验入库；无OpenAI API调用。

## 1. 研究目标

研究主要证券交易城市在开盘前和交易时段内的天气状况，是否与对应股票指数当日收益率、市场广度、波动率及辅助评分存在统计关系。本研究是观察性研究，交易所所在地天气仅是天气环境代理变量，不能直接建立因果关系。

## 2. 研究对象

北京—北证50、上海—上证指数、深圳—深证成指、东京—TOPIX、孟买—NIFTY 50、伦敦—FTSE 100、法兰克福—DAX、纽约—S&P 500。详细时区与窗口见`config/markets.json`。

## 3. 采集周期

统一ChatGPT计划任务北京时间周一至周六07:00运行：

- 周二至周六：采集各地区最近一个已经完整结束的当地交易日；
- 周一：汇总上一完整自然周的每日输出，按官方日历判断预期记录，补录缺失或损坏日期，并生成完整Codex周包。

任务不得机械地把“昨天”视为交易日，必须核对官方日历、提前收盘、特殊交易和临时休市。

## 4. 天气窗口与原始变量

每城保存`pre_open`、`trading_session`、`full_day`。正式评分至少优先收集：体感温度、降水、云量或日照、AQI、最大阵风。PM2.5作为跨国空气质量分析的优先原始变量。温度、湿度和平均风速可作为扩展字段。

历史日期优先使用`observed`或可靠`reported`数据；不得将`forecast`作为完成历史记录导入。

## 5. 市场原始变量

开市记录必须至少包含：前收盘、开盘、最高、最低、收盘、收盘收益率和日内振幅。成交量、成交额、上涨数、下跌数和不变数为可选增强字段。休市记录不得保存伪造价格。

## 6. 数据源

优先级：官方交易所、指数提供方或官方气象机构 → 稳定专业数据服务 → 权威财经或天气媒体。每个非空值必须引用`source_id`，且`source_id`必须映射到带URL、发布方和访问时间的来源对象。`calendar_checked=true`时，市场必须通过`calendar_source_ids`保存日历核对来源。来源冲突必须记录，不能暗中挑选更符合预期的数字。

离线程序检查URL语法和引用闭合，但不自动证明URL可访问、网页内容支持对应数值或来源真实权威；`calendar_source_ids`同样只是可审计证据指针，不代表程序已经理解并重新核验网页内容。

## 7. 传输与正式数据分离

ChatGPT输出`Daily Transport Export v1.0.2`，使用紧凑字段减少周包长度。该格式不是正式数据库记录。

Codex每周将传输格式转换为`Regional Daily Record`：

1. 保存周包原文；
2. 运行传输Schema和语义校验；
3. 检查来源引用、日期、区域和主要市场；
4. 用Python复算天气与市场评分；
5. 生成规范文件或拒绝记录；
6. 运行测试后创建草稿PR。

周包清单同时保存`expected_records`、`found_records`、`unresolved_records`等明确的`region/target_date`集合。程序验证ISO自然周、覆盖区间和集合关系，但`expected_records`仍是经外部官方日历核对后的周包声明，不是程序自行推导的全球官方交易日历。

## 8. 评分

正式评分严格执行`rubrics/`和`scripts/scoring.py`。ChatGPT可以给出自然语言摘要，但不能以其自行计算的分数替代Python复算结果。评分版本保持`1.0.0`；任何规则变化必须升级版本。

## 9. 幂等性与修订

路径固定为`data/raw/<region>/<target_date>.json`。

- 文件不存在：创建revision 1；
- 内容在忽略运行时间等操作元数据后相同：跳过；
- 内容不同：默认拒绝，不自动覆盖；
- 人工核对确认来源更权威后，使用`--allow-revision`生成新revision并保留Git历史。

## 10. 漏跑与补录

周一任务应检查上一周预期记录。日常输出不可访问、损坏或缺失时，可重新搜索历史数据并标记`collection_mode=backfill`、实际`lag_days`和原因。找不到可靠细粒度数据时使用`null`和`unavailable`，不得为完整率虚构。

仓库脚本产生的工作日缺失列表只是候选，Codex必须再次核对官方交易日历，避免把法定休市误判成漏跑。

`WEEKLY_AGGREGATION_INCOMPLETE`允许合格记录部分入库。原文保存在`inbox/raw`，状态副本保存在`inbox/partial`，并生成补录请求；只有没有未解决或拒绝项的完整周包才进入`inbox/processed`。

## 11. 成本边界

V1.0.2不得依赖`OPENAI_API_KEY`、OpenAI Responses API或其他额外付费模型API。GitHub Actions只负责PR中的代码和数据验证，不负责采集。ChatGPT和Codex使用用户现有Plus套餐内能力，仍受套餐用量限制。

## 12. 预注册分析与阶段门槛

- 分数使用Spearman；连续变量与收益率报告Pearson；
- 正式回归使用Newey–West稳健标准误；
- 多重检验使用Benjamini–Hochberg；
- 0—20个交易日只做工程质量验收；
- 20—60个交易日仅做探索性描述；
- 约252个交易日后进行第一轮正式统计分析。

## 13. V1.0.2验收指标

- 日常任务预期产出率≥95%；
- 周包包含率与清单一致，无法包含时明确列出缺失；
- 传输JSON可提取率100%；
- 正式记录Schema与评分复算通过率100%；
- 每个非空指标来源覆盖率100%；
- 重复规范文件0；
- 严重日期或时区错误0；
- 可恢复缺失补录成功率≥90%；
- OpenAI API额外费用为0。
