# AEX series definition

No AEX series can be recommended in this run. The official directory describes daily *ultimo* and average stock-index values, but the required JSON payloads were not retrievable, so the actual AEX code, whether a candidate is a price or total-return index, dividend treatment, unit, and the precise end-of-period versus average flag could not be read from source metadata.

Recommended series: `not_determined`.

Status: `technical_access_blocked`. The probe does not substitute a third-party quote, and it does not treat a field name or the catalogue description as proof of an official AEX close.
